import react from "React";
import {render} from "react-dom";
import App from "./App";

const styles = {
  fonntFamily :"sans-serif",
  textAlign: 'center' 
}

render(<App />, document.getElementsById('root'));